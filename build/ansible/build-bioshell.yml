---
- name: Build BioShell base image
  hosts: all
  become: true

  vars_files:
    - "vars/tool-versions.yml"
    - "vars/{{ platform }}.yml"

  roles:
    - common
    - singularity
    - nextflow
    - rstudio
    - cvmfs
    - shelley-bio